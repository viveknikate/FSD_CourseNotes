<%- include('partials/header') -%>
     <h1>Home</h1>
     <p>
          <%= homeText %>
     </p>
     <div style="height: 61vh; overflow: auto; background-color: #E3F4F4; padding: 30px; border-radius: 5px;">     
          <!-- <% for(let i=0;i<content.length; i++){ %>
               <h1> <%= content[i].title %> </h1>
               <p> <%= content[i].body %> </p>
               <% } %> -->

               <% content.forEach((i,index)=>{ %>
                    <h1><%= index+1 %>.<%= i.postTitle %></h1>
                    <!-- <p><%= i.postContent.slice(0,120) %>...</p> -->
                    <p>
                         <%= i.postContent.substring(0,100) %>
                         <a href="/posts/<%= i._id %>"> Read More...</a>
                    </p>
                    <center>
                         <div style="border-bottom:dotted rgb(84, 71, 71) 2px;"></div>
                    </center>
               <% }) %>
               
     <!-- 
          <% let allPosts = [] %>
          <% for(let i=0; i < allPosts.length; i++ ){ %>
               <h1><%= allPosts[i].title %></h1>
               <p>
                    <%= allPosts[i].Desc %>
               </p>
          <% } %>

          using Advance for loop

          <% allPosts.forEach( (i)=>{ %>
               <h1><%= i.title %> </h1>
               <p>
                    <%= i.Desc %>
               </p>
          <% }) %>  -->
               
          <center>
               <button class="btn btn-outline-primary" style="border-radius: 20%;font-size: 3em; margin: 20px;">
                    <a href="/compose" style="text-decoration: none;">+</a>
               </button>
          </center>
     </div>
<%- include('partials/footer') -%>


